{
  "name": "adriano-to-the-star-mobile",
  "version": "1.0.0",
  "description": "Mobile app for Adriano To The Star - Interstellar Travel Agency",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Build web assets first, then sync to native'",
    "sync": "npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build:ios": "npx cap sync ios && npx cap build ios",
    "build:android": "npx cap sync android && npx cap build android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0",
    "@capacitor/push-notifications": "^5.0.0",
    "@capacitor/camera": "^5.0.0",
    "@capacitor/geolocation": "^5.0.0",
    "@capacitor/app": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}

